package task5;

import core.Node;

/**
 * Client
 */
public class ProductService {
    /**
     * 
     * @param start
     * @return the product of values inside the Nodes of the list starting at Node passed.
     * Return 1 if list is empty (start is null)
     */
    public static int product(Node start) {
        return 1; //TODO
    }
}